# 项目结构问题与改造建议

> 审查范围：仓库全部 46 个 Go 文件（约 3000 行）+ 配置 + 目录卫生
> 结论概览：分层骨架方向正确，但 `user` 之外的链路只走通了一半——`file`/`agent` 的业务逻辑堆在 handler 里，`memory` 是 0 引用的孤岛。有 3 处凭证明文、8 处会 panic 或静默失效的缺陷。

---

## 〇、优先级总览

| 级别 | 事项 | 影响 |
|---|---|---|
| P0 | 轮换泄露的凭证、`git init`、清理杂物 | 安全事故 + 无法回滚 |
| P1-1 | 三个 indexer 合并成一个 | 360 行重复代码，改一处要改三处 |
| P1-2 | 上传流程从 handler 下沉到 service | 148 行业务逻辑压在 HTTP 层 |
| P1-3 | `utils/silo` 迁到 `internal/`，`utils/jwt` 解依赖 | 依赖方向错误 |
| P1-4 | 配置统一（yaml / env 二选一） | 两套配置源并存 |
| P1-5 | `memory` 接进 agent 或删除 | 半成品悬挂 |
| P2-1 | 修 8 个缺陷（见第四节） | 会 panic / 静默失效 |
| P2-2 | 生产代码清理（注释块、调试打印、拼写） | 可读性 |

---

## 一、P0：安全与仓库卫生

### 1.1 明文凭证（最紧急）

`config/config.yaml` 中以下三项是明文：

| 位置 | 内容 |
|---|---|
| `jwt.key` | JWT 签名密钥 |
| `embedder.api_key` | Embedding 服务 API Key |
| `mysql.password` | MySQL root 密码 |

`.env`（309 字节）也是明文，且被 `cmd/server/cmd.go:24` 的 `gotenv.Load()` 加载。

另外 `cmd/server/cmd.go:31` 有 `fmt.Printf("%+v", config.Conf)`，启动时会把上述全部凭证打印到 stdout。

**推荐改法：**

1. **立刻轮换这三个凭证**（API Key 视为已泄露，去服务商后台重置；MySQL 改密码；JWT key 重新生成）。
2. 配置分层：`config/config.yaml` 只放非敏感项并提交；敏感项走环境变量覆盖：

```go
// internal/config/config.go
// 需要额外 import "strings"
func Init() error {
    viper.SetConfigFile("config/config.yaml")
    if err := viper.ReadInConfig(); err != nil {
        return err
    }

    viper.SetEnvPrefix("RAG")                              // 键名加前缀：mysql.password → RAG_MYSQL_PASSWORD
    viper.SetEnvKeyReplacer(strings.NewReplacer(".", "_")) // 环境变量名不能带点
    viper.AutomaticEnv()                                   // 每次取值都先查环境变量（优先级高于 yaml）

    // ⚠️ 这一段不能省。viper.Unmarshal 是遍历 AllKeys() 来取值的，
    // 而 AutomaticEnv 不往 AllKeys() 里注册任何键（它只置一个标志位）。
    // 后果：yaml 里已存在的键（mysql.password 等）能被环境变量覆盖；
    //      只在环境变量里、yaml 里没有占位的键会被静默忽略，字段保持零值。
    for _, key := range []string{
        "mysql.password",
        "jwt.key",
        "embedder.api_key",
        "s3.endpoint", "s3.access_key", "s3.secret",
        "multimodal.base_url", "multimodal.api_key", "multimodal.model",
        "chat.base_url", "chat.api_key", "chat.model",
    } {
        if err := viper.BindEnv(key); err != nil {
            return err
        }
    }
    return viper.Unmarshal(Conf)
}
```

`BindEnv("s3.endpoint")` 的单参数形式会自动拼成 `RAG_S3_ENDPOINT`，与上面三行的命名规则完全一致，不用手写第二遍。上面这些键名要和 2.4 节的 `Config` 结构体保持一致。

**验证方式：**

```bash
RAG_MYSQL_PASSWORD=test123 go run .
```

`cmd/server/cmd.go:31` 那行打印 `config.Conf` 正好可以用来确认覆盖是否生效——验证完按第 3 条删掉。

3. 删掉 `cmd/server/cmd.go:31` 那行 `fmt.Printf`，或改为只打印非敏感字段（如 `config.Conf.Qdrant`）。
4. `.env` 加入 `.gitignore`，仓库里只留 `.env.example`。

### 1.2 仓库不是 git 仓库

`git status` 报 `fatal: not a git repository`。没有版本控制，意味着上一条的凭证轮换之后，也没有任何回滚能力。

**推荐改法：**

```bash
git init
```

创建 `.gitignore`：

```gitignore
.idea/
.env
*.md.bak
tmp_*/
```

### 1.3 需要从仓库删除的文件

| 路径 | 说明 |
|---|---|
| `tmp_emptyupsert/main.go` | 一次性诊断脚本（测试 Qdrant 空 points upsert），会污染 `go build ./...` 的入口 |
| `kimi-export-session_-20260915-114902.md` | 226KB 的 AI 对话导出，4500 行，含本项目上下文 |
| `.idea/`、`internal/.idea/` | IDE 配置，且 `internal/.idea` 混在 Go 包目录里 |
| `.env` | 见 1.1 |

诊断脚本如果还想保留，放到 `scripts/` 或改造成 `_test.go`（`tmp_` 前缀目录会被 Go 工具链忽略，但 `tmp_emptyupsert` 不会）。

---

## 二、P1：结构改造

### 2.1 三个 indexer 合并成一个（重复代码最严重的地方）

**现状：** 以下三个文件各 119~120 行，除了包名和 `GetType()` 的返回值，**逐行相同**（连注释都一样），差异只有最后那个字符串：

| 文件 | 行数 | `GetType()` 返回 | 是否被使用 |
|---|---|---|---|
| `internal/indexer/qdrant.go` | 120 | `"qdrant"` | ❌ 无人 import（死代码） |
| `internal/indexer/knowledge/knowledge.go` | 119 | `"knowledge"` | ✅ `handler/file.go:58` |
| `internal/indexer/object/object.go` | 119 | `"object"` | ✅ `handler/file.go:65` |

**推荐改法：** 只保留 `internal/indexer` 一个包，用已有的 `WithCollectionName` option 区分集合，删除 `knowledge/` 和 `object/` 两个子目录。

```go
// internal/indexer/qdrant.go（合并后）
package indexer

type Config struct {
    Client     *qdrant.Client
    Collection string
    Dimension  int
}

// 三个包原本的差异只有 GetType() 的返回值和默认集合名，
// 前者用构造参数，后者用 WithCollectionName 即可覆盖。
```

调用侧（`handler/file.go`，注意这块同时会在 2.2 一并下沉）：

```go
knowledgeIdx := indexer.New(&indexer.Config{Client: h.qdrant, Dimension: 2048, Collection: username})
objectIdx := indexer.New(&indexer.Config{Client: h.qdrant, Dimension: 2048, Collection: username})
// 使用时再覆盖：
objectIdx.Store(ctx, docs, indexer.WithCollectionName(username+"file"))
```

顺带清理：三个包里的 `topK`、`fileType` 字段和对应的 `WithTopK`/`WithFileType` option **从未被 `Store` 使用**，合并时直接删掉。

### 2.2 上传流程从 handler 下沉到 service

**现状：** `internal/handler/file.go:47-194` 的 `UpLoadFile` 有 148 行，在 HTTP 层做了：

- new 出 parser / transformer / 两个 indexer（第 49-71 行）
- 循环里调 embedder、base64 编码、按 `image/` 前缀分支（第 121-175 行）
- 手工拼 `schema.Document`、手工转换 `[]float64 → []float32`（第 133-136、169-173 行）

而 `internal/service/file.go` 只有 91 行，且 `ParseFile` 走的是**另一条只支持 PDF 的路径**（`parser.NewPDFParser`），与 handler 里支持 html/docx/pdf 的 `ExtParser` 是两套实现。

**推荐改法：** handler 只做「绑定参数 → 调 service → 映射错误 → 写响应」。

```go
// internal/service/file.go
type UploadResult struct {
    Succeeded []string
    Failed    map[string]string // filename -> reason
}

func (svc *FileService) Upload(
    ctx context.Context,
    files []*multipart.FileHeader,
    username, prefix, collection string,
) (*UploadResult, error) {
    // 解析、切分、向量化、入库全部在这里
}
```

```go
// internal/handler/file.go —— 改造后
func (h *FileHandler) UpLoadFile(c *gin.Context) {
    form, err := c.MultipartForm()
    if err != nil {
        response.Fail(c, http.StatusBadRequest, nil, "解析表单失败: "+err.Error())
        return
    }
    files := form.File["files"]
    prefix, collection := form.Value["filePrefix"], form.Value["collectionName"]
    if len(files) == 0 || len(prefix) == 0 || len(collection) == 0 {
        response.Fail(c, http.StatusBadRequest, nil, "缺少 files / filePrefix / collectionName 字段")
        return
    }
    res, err := h.svc.Upload(c.Request.Context(), files,
        c.GetString("username"), prefix[0], collection[0])
    if err != nil {
        response.Fail(c, http.StatusInternalServerError, nil, err.Error())
        return
    }
    response.Success(c, res)
}
```

同时 `parser.NewExtParser()` 应该在 `cmd/server/cmd.go` 里构造一次注入，不要在每次请求里重建（`handler/file.go:49` 和 `transformer.NewTransformer` 都是每请求重建，后者还会重建 semantic splitter）。

### 2.3 `utils/` 里装的是业务代码

| 包 | 被引用次数 | 问题 |
|---|---|---|
| `utils/silo` | 8 | 不是工具：硬编码 `cn-north-1`、bucket 名即用户名、内置文件类型嗅探策略。是项目里耦合最广的包 |
| `utils/jwt` | 2 | `jwt.go:5` 反向 import `rag/internal/config`，方向错误，且导致无法独立测试/复用 |
| `utils/response` | 3 | 与 gin 强绑定，和其它纯函数的 utils 混在一起 |
| `utils/splitter` | 0 | 死代码，无人引用 |
| `utils/bm25`、`utils/jieba` | 1、1 | 合理，保留 |

**判断标准：** utils 里的东西应该能原样复制到别的项目而不改动。按此标准只有 `password`、`bm25`、`jieba` 够格。

**推荐改法：**

1. `utils/silo` → `internal/storage/silo`（`internal/` 天然阻止外部导入，语义也更准确）。GoLand 可以直接 Refactor → Move，8 处 import 会自动更新。
2. `utils/jwt` 解除对全局配置的依赖，改为显式传参：

```go
func GenerateToken(username string, cfg Config) (string, error)
func ParseToken(tokenString string, cfg Config) (*MyClaims, error)

type Config struct {
    Key  string
    Hour int
}
```

3. `utils/splitter` 删除（或移到 `internal/rag/splitter` 备用）。
4. `utils/response` 保持不动，它确实服务整个 HTTP 层。

### 2.4 配置读取统一

**现状：** 两套并行。

| 来源 | 覆盖内容 |
|---|---|
| `config.Conf`（viper → config.yaml） | jwt、embedder、mysql、qdrant |
| `os.Getenv` | S3（`SiloEndPoint`/`SiloAccessKey`/`SiloSecret`）、多模态（`MultiModalBaseUrl`/`ApiKey`/`Model`）、DeepSeek（`DEEPSEEK_*`） |

`config.Config` 结构体的 tag 也是混用的：`Jwt`/`Embedder` 用 `mapstructure` 或 `json`，`Qdrant`/`Mysql` 完全没有 tag（靠 viper 的大小写不敏感匹配侥幸工作）。

**推荐改法：** 全部收进 `config.Conf`，补全缺的段：

```go
type Config struct {
    JWT        JWT        `mapstructure:"jwt"`
    Qdrant     Qdrant     `mapstructure:"qdrant"`
    Mysql      Mysql      `mapstructure:"mysql"`
    Embedder   Embedder   `mapstructure:"embedder"`
    MultiModal MultiModal `mapstructure:"multimodal"`
    Chat       Chat       `mapstructure:"chat"`
    S3         S3         `mapstructure:"s3"`
}
```

然后删掉 `cmd/server/cmd.go` 和 `internal/agent/agent.go` 里所有 `os.Getenv`。这些键里有的在 yaml 里没有对应项（S3、多模态、DeepSeek），改完后要同步加进 1.1 节的 `BindEnv` 列表，否则会静默读到零值——原因见 1.1 的说明。

### 2.5 `internal/memory` 接进 agent，或删除

**现状：** 整个包 **0 个引用**。配套的 `model.Session`/`Message`/`Summary` 也只有一个 `AutoMigrate(&model.User{})`（`database.go:27`），三张表根本没建。因为从没被调用过，里面的缺陷（见第四节）没人发现。

**推荐改法（接进去）：** 在 `AgentService.Ask` 的两端接线：

```go
func (svc *AgentService) Ask(ctx context.Context, query, userID, sessionID string, topK int) (<-chan dto.StreamChunk, error) {
    history, _ := svc.session.GetMessages(ctx, userID, sessionID, 20)
    messages := append(historyToMessages(history), schema.UserMessage(query))
    // ... runner.Run(ctx, messages) ...
    // 在 goroutine 里流式结束后：
    _ = svc.session.AppendMessage(ctx, userID, sessionID, model.Message{...})
}
```

同时：

- 把 `SessionManager` 的构造放进 `cmd/server/cmd.go` 注入（现在 `AgentService` 只拿到 `*qdrant.Client` 等，没有 db）。
- `model.Message` 与 `schema.AgenticMessage` 直接映射到数据库列是行不通的（见 4.6），需要加 serializer 或用单独的持久化结构。
- 补上 `AutoMigrate`。

**推荐改法（暂时不做）：** 整个 `internal/memory/` 目录和 `model/{session,message,summary}.go` 一起删掉，等真要实现多轮对话时再从 git 历史里取回。**不要留着随时会被误用的半成品。**

### 2.6 依赖装配：绕了一圈被丢掉的 chatmodel

**现状：** `cmd/server/cmd.go:64-68` 创建了 `chatmodel`，经 `router.InitAgentRouter` → `handler.NewAgentHandler` → `service.NewAgentService` 一路传参，在 `service/agetn.go:30` 存进结构体后**再也没有被使用过**；`agent.NewAgent`（`internal/agent/agent.go:32-36`）内部又用 `os.Getenv` 重新创建了一个。

**推荐改法：** 让 agent 接收 model 参数，删掉内部的 `os.Getenv`：

```go
// internal/agent/agent.go
func NewAgent(ctx context.Context, model model.ChatModel, re retriever.Retriever, silo *silo.Silo) (adk.ResumableAgent, error) {
    // 去掉内部 new chatmodel
}

// internal/service/agetn.go
agent, err := agent.NewAgent(ctx, svc.chatmodel, re, svc.silo)
```

顺带：`internal/agent/agent.go:38` 和 `:40` 两处 `err` 被丢弃（`agent, _ := adk.NewTypedChatModelAgent(...)`），也应该处理。

---

## 三、推荐的目标结构

```
.
├── main.go                     # 7 行，保持
├── cmd/server/cmd.go           # 唯一的装配点，保持（补充 parser/transformer 的构造）
├── config/
│   ├── config.yaml             # 只放非敏感项，可提交
│   └── config.example.yaml
├── internal/
│   ├── config/                 # 唯一配置来源，含 env 覆盖
│   ├── router/                 # 保持
│   ├── middleware/             # 保持
│   ├── handler/                # 只做参数绑定与响应映射
│   ├── service/                # 所有业务流程（upload / ask 都在这里）
│   ├── dao/ model/ dto/        # 保持，dto 补上 file 相关
│   ├── storage/silo/           # ← 从 utils/silo 迁入
│   ├── rag/
│   │   ├── parser/             # PDF 三种实现收敛为一
│   │   ├── transformer/        # PDF transformer 的裸类型断言要处理
│   │   ├── splitter/           # ← 从 utils/splitter 迁入（或删除）
│   │   ├── embedder/           # text.go + multi.go（改名见第四节）
│   │   ├── indexer/            # 只留一个 Qdrant indexer
│   │   └── retriever/          # 含 bm25 混合检索
│   ├── agent/ memory/ tools/   # agent、memory、tools 内聚
│   └── ...
└── utils/                      # 只剩 password / bm25 / jieba / response
```

补充：`internal/` 下现有 17 个一级目录、3000 行代码，平均每包 170 行——`memory`、`splitter`、顶层 `indexer` 这三个包都是「先建目录、代码没接上」。**先集中把一条链路跑通再拆包**，会比现在好维护。

---

## 四、P2：缺陷清单（按严重程度排序）

### 4.1 `internal/parser/pdf.go` —— nil 指针 panic（扫描版 PDF 必崩）

```go
// 第 82 行
pages := make([]*schema.Document, numPage)  // 元素全是 nil 指针
// 第 102-106 行
pages[i].Content = text                      // nil 解引用 → panic
pages[i].MetaData["page"] = i                // 即使不是 nil，往 nil map 写也会 panic
```

触发路径：`parser/pdf.go:177` 的降级分支——当某页没有任何文本（扫描版 PDF）时调用 `ParsePDFByFitz`。

**改法：**

```go
pages := make([]*schema.Document, numPage)
for i := range pages {
    pages[i] = &schema.Document{
        ID:       uuid.New().String(),
        MetaData: map[string]any{"page": i, "source": first.Metadata()["title"]},
    }
}
// worker 内只需：pages[i].Content = text
```

另外该文件里还有：未使用的 `ParsePDFByEino`（第 236-260 行）、大段注释掉的代码（第 205-233 行）、`Parse` 方法上贴的注释写着 `Load`（第 197 行）、`log.Fatal` 出现在库函数里（第 180 行，应该返回 error 而不是终止进程）。

### 4.2 `internal/retriever/qdrant.go` —— `opts` 被完全忽略，`WithCollectionName` 和 TopK 全链路失效

`Retrieve` 从头到尾没有调用 `retriever.GetImplSpecificOptions(r, opts...)`，所以：

- `tools/file.go:50` 的 `retriever.WithCollectionName(input.Collection)` **不生效** → 文件检索永远查用户集合，查不到 `usernamefile` 集合。
- `tools/rag.go:43` 传的 `topK` **不生效** → `QueryPoints` 也没设 `Limit`（第 68-73 行），实际用的是 Qdrant 默认值 10。

**改法：**

```go
func (r *QdrantRetriever) Retrieve(ctx context.Context, query string, opts ...retriever.Option) ([]*schema.Document, error) {
    retriever.GetImplSpecificOptions(r, opts...)   // ← 补上
    // ...
    searchResult, err := r.client.Query(ctx, &qdrant.QueryPoints{
        CollectionName: r.collection,
        Query:          qdrant.NewQuery(vec32...),
        Limit:          new(uint64(r.topK)),  // ← 补上（该版本没有 NewLimit，Limit 就是 *uint64）
        WithVectors:    qdrant.NewWithVectors(true),
        WithPayload:    qdrant.NewWithPayload(true),
    })
```

### 4.3 `internal/tools/file.go:56-58` —— 类型断言 panic + payload 键不匹配

```go
document.MetaData["filename"].(string)   // 断言失败 → panic
```

根因在写入与读取的键不一致：

| 环节 | 行为 |
|---|---|
| 写入 `service/file.go:46-53` | `MetaData` 设 `filename`/`filetype`/`bucket`，indexer 把它们**扁平**写进 payload |
| 读取 `retriever/qdrant.go:87-91` | 只从 payload 取 `content` 和 `metadata` 两个键 |

`metadata` 键在 payload 里根本不存在，所以 `doc.MetaData` 恒为 `map[string]any{}`（空 map），断言必然 panic。

**改法一（推荐）：** 读取时把 payload 的其余键全部还原进 `MetaData`：

```go
for _, result := range searchResult {
    doc := &schema.Document{
        ID:       result.Id.GetUuid(),
        Content:  result.Payload["content"].GetStringValue(),
        MetaData: map[string]any{},
    }
    for k, v := range result.Payload {
        if k == "content" {
            continue
        }
        doc.MetaData[k] = payloadValue(v)
    }
    doc.WithScore(float64(result.Score))
    docs = append(docs, doc)
}
```

该客户端版本**没有** `Value.AsInterface()` 这类便捷方法（已确认 v1.19.2 无此 API），也没有 `Value.GetKind()`。但写入侧（`indexer.Store` → `qdrant.NewValueMap`）塞进 payload 的 `filename`/`filetype`/`bucket` 都是字符串，所以直接取字符串即可：

```go
for k, v := range result.Payload {
    if k == "content" {
        continue
    }
    doc.MetaData[k] = v.GetStringValue()   // 非字符串类型会返回零值 ""
}
```

如果以后 payload 里要放非字符串（数字、布尔、嵌套结构），再按 oneof 展开——注意 `Kind` 字段的静态类型 `isValue_Kind` 是未导出的，但可以正常做类型 switch：

```go
func payloadValue(v *qdrant.Value) any {
    switch val := v.Kind.(type) {
    case *qdrant.Value_StringValue:
        return val.StringValue
    case *qdrant.Value_IntegerValue:
        return val.IntegerValue
    case *qdrant.Value_DoubleValue:
        return val.DoubleValue
    case *qdrant.Value_BoolValue:
        return val.BoolValue
    default:
        return v.GetStringValue()   // struct / list / null 当前用不到
    }
}
```

**改法二：** `tools/file.go` 改用带 ok 的断言：

```go
if v, ok := document.MetaData["filename"].(string); ok {
    f.FileName = v
}
```

两边都做，才是最稳的。

### 4.4 `internal/memory/short.go` —— 窗口语义与注释相反

| 行 | 代码 | 问题 |
|---|---|---|
| 47 | `LRange(ctx, key, 1, 20)` | 从下标 **1** 开始，第 1 条消息永远读不到（应为 `0, -1`） |
| 33 | `LTrim(ctx, key, -20, -1)` | 保留最后 20 条 ✅ |
| 78/89 | `LTrim(ctx, key, 20, -1)` | 注释说"删除队列里的消息"，实际是**保留**下标 20 之后的内容；只是因为列表恰好 ≤20 条才等价于清空。想清空应该用 `Del` |

另外 `ShortMessageManager.Window` 字段（第 24 行）**从未被使用**，窗口大小 20 硬编码在 4 处。

**改法：**

```go
const defaultWindow = 20   // 或者用 Window 字段，但只留一个来源

func (s *ShortMessageManager) GetMessages(ctx context.Context, sessionID string) ([]*schema.AgenticMessage, error) {
    values, err := s.redis.LRange(ctx, key, 0, -1).Result()   // 全部取出，写入时已经 Trim 过
    // ...
}

func (s *ShortMessageManager) ClearMessages(ctx context.Context, sessionID string) error {
    return s.redis.Del(ctx, sessionID+"chat").Err()           // 语义明确
}
```

### 4.5 `internal/memory/session.go` —— 表名不一致 + 漏 Where

| 行 | 代码 | 问题 |
|---|---|---|
| 61 | `Table("session").Create(session)` | 表名 `session` |
| 71 | `Table("session").Update("status", false)` | **没有 Where**，GORM 默认会直接报 `WHERE conditions required`，此方法永远失败 |
| 80 | `Table("sesssion").Scan(&session)` | 表名拼错（三个 s），且**没有 Where**，会随机取一行 |
| 87 | `Table("messages").Where(...).Limit(limit).Scan(&messages)` | 表名 `messages`，而 `ListSessions` 用的是 `sessions` |

统一表名（建议直接用 `model.Session{}.TableName()` 或 `gorm:"table:session"` tag），并补上条件：

```go
func (s *SessionManager) DeleteSession(ctx context.Context, userID, sessionID string) error {
    return s.db.Model(&model.Session{}).
        Where("id = ? AND user_id = ?", sessionID, userID).
        Update("status", false).Error
}
```

### 4.6 `internal/model/message.go` —— 无法映射到数据库列

```go
type Message struct {
    SessionID string
    Msg       *schema.AgenticMessage   // eino 类型，GORM 不知道怎么存
}
```

**改法：** 加 serializer，或者持久化用独立结构：

```go
type Message struct {
    ID        uint                    `gorm:"primaryKey"`
    SessionID string                  `gorm:"index;size:64"`
    Role      string                  `gorm:"size:16"`
    Content   string                  `gorm:"type:text"`
    Extra     datatypes.JSON          `gorm:"type:json"`  // tool_calls 等
}
```
（`gorm.io/datatypes` 需要额外引入；若不想加依赖，`Extra` 用 `string` + 手工 `json.Marshal` 也可以。）

### 4.7 `internal/handler/user.go:61` —— error 被丢弃 + 职责越界

```go
token, err := jwt.GenerateToken(req.Username)   // err 未检查
c.JSON(200, map[string]any{ "data": map[string]any{"token": token} })
```

签发 token 失败时会返回 **200 + 空 token**。而且 token 签发属于业务逻辑，不该在 handler 里。

**改法：** 移到 `UserService.Login`，返回 `(dto.LoginRes, error)`：

```go
func (svc *UserService) Login(ctx context.Context, req dto.LoginReq) (*dto.LoginRes, error) {
    // 校验密码...
    token, err := jwt.GenerateToken(req.Username, svc.jwtCfg)
    if err != nil {
        return nil, fmt.Errorf("签发 token 失败: %w", err)
    }
    return &dto.LoginRes{Token: token}, nil
}
```

### 4.8 `internal/handler/file.go:88-91` —— 缺参校验后没有 return

```go
collectionName := form.Value["collectionName"]
if len(collectionName) == 0 {
    response.Fail(c, http.StatusBadRequest, nil, "缺少collectionName字段")
    // ← 这里少了 return，会继续往下执行，之后再写一次响应头
}
```

**改法：** 补 `return`。同时统一响应写法——同一个文件里 `c.JSON(` 出现了 11 次、`response.Fail(` 6 次、`gin.H{` 3 次，风格混杂且状态码不一致（有 `500`、`http.StatusInternalServerError`、`http.StatusBadRequest` 三种写法）。建议全部收敛到 `response.Success` / `response.Fail`。

---

## 五、其它值得顺手处理的

| 位置 | 问题 |
|---|---|
| `internal/service/agetn.go:52-66` | 一段 15 行的 prompt 硬编码在业务逻辑中间，且是带大量尾随空格的单行字符串。建议移到 `internal/agent/prompt.go` 或用 `//go:embed` |
| `internal/service/agetn.go:122-142` | 20 行注释掉的旧实现（`result.Recv()` 版本），且当前实现从未调用 `result.Close()`，流式迭代器存在资源泄漏 |
| `internal/service/agetn.go:30` | `chatmodel` 字段存入后未使用（见 2.6） |
| `internal/service/agetn.go:35` | `Ask` 里 `ctx := context.Background()` 覆盖了外部传入的 ctx，客户端断开后不会取消下游调用 |
| `internal/agent/agent.go:72` | `MaxIterations: 10000`，ReAct 循环上限值过大，建议 10~20 |
| `internal/agent/agent.go:38,40` | 两处 error 被丢弃 |
| `internal/parser/parser.go:19` | `htmlParser, _ := html.NewParser(...)` error 被丢弃 |
| `internal/transformer/transformer.go:27-41` | 4 处 `_` 丢弃 error（`markdown`/`html`/`recursive`/`semantic`） |
| `internal/transformer/pdf.go:31,42,76` | `doc.MetaData["page"].(int)` 无保护断言，上游 MetaData 一旦没设 page 就 panic |
| `internal/retriever/qdrant.go:98-107` | BM25 归一化分数与余弦相似度等权相加（权重写死 1.0），没有可配置项 |
| `internal/indexer/*` 三处 | `WithTopK`/`WithFileType` 是死 option，`topK`/`fileType` 字段从未被 `Store` 读取 |
| `cmd/server/cmd.go:31` | `fmt.Printf("%+v", config.Conf)` 打印全部凭证 |
| `cmd/server/cmd.go:74-78` | `log.Fatal(err)` 之后还有 `os.Exit(1)`，不可达代码；且 `ListenAndServe` 应区分 `http.ErrServerClosed` |
| `cmd/server/cmd.go:24` | `gotenv.Load()` 失败直接 `panic`，与其它地方 `log.Fatal` 不一致 |

### 拼写与命名（会被 IDE 全局重命名一次性修掉）

| 位置 | 现状 | 应为 |
|---|---|---|
| `internal/service/agetn.go` | 文件名 | `agent.go` |
| `internal/handler/agent.go:16,20,21,26` | `AgentHnadler` | `AgentHandler` |
| `internal/embedder/mutl.go` | 文件名 | `multi.go` |
| `internal/dto/file.go:5` | `json:"bucktname"` | `json:"bucket_name"` |
| `internal/memory/session.go:80` | `"sesssion"` | `"session"` |
| `internal/service/user.go:43` | `hashpassworrd` | `hashpassword` |
| `utils/jwt/jwt.go:12` | `Username string \`json:"id"\`` | `json:"username"` |
| `internal/service/user.go:47` | 打印 bcrypt hash 到 stdout | 删除 |
| `internal/handler/file.go:97,190` | `failfile` | `failedFiles` |
| 多处 | 局部变量首字母大写（`Indexer :=`、`FileHandler :=`、`Textembedder :=`） | 小写 |

---

## 六、建议的执行顺序

**第 1 批（当天，安全）**
- [ ] 轮换 JWT key / Embedding API Key / MySQL 密码
- [ ] `git init` + `.gitignore`，提交一次当前状态作为基线
- [ ] 删 `tmp_emptyupsert/`、`kimi-export-session-*.md`、两个 `.idea/`
- [ ] 删 `cmd/server/cmd.go:31` 的 `fmt.Printf`

**第 2 批（结构性，收益最大）**
- [ ] 合并三个 indexer 为一个包，删掉顶层死代码和 `utils/splitter`
- [ ] 上传流程下沉到 `FileService.Upload`，handler 只留参数绑定
- [ ] `parser.NewExtParser()` 和 `NewTransformer()` 改为启动时构造一次并注入

**第 3 批（修缺陷）**
- [ ] 修 4.1（PDF nil panic）→ 4.2（retriever opts）→ 4.3（MetaData 断言）→ 4.8（漏 return）→ 4.5/4.4（memory 表名与窗口）
- [ ] 全仓库搜 `_ :=` 和 `.(\w+)$` 形式的裸类型断言，逐个加保护

**第 4 批（一致性）**
- [ ] 配置统一到 `config.Conf`，补全结构体 tag，加 viper 环境变量覆盖
- [ ] `utils/silo` → `internal/storage/silo`，`utils/jwt` 改显式传参
- [ ] 跑一遍 IDE 的全局重命名修拼写

**第 5 批（决定 memory 去留）**
- [ ] 接入 agent（补 `AutoMigrate`、改 `model.Message` 为可持久化结构），或整个删除

**长期**
- [ ] 至少给 `service`/`utils/bm25` 补几个单测（当前 0 个测试文件）
- [ ] 加 `Makefile`（`build`/`run`/`lint`/`test`）和 `README.md`
- [ ] 引入 `golangci-lint`，`go build`/`go vet` 目前是干净的，正好作为基线
