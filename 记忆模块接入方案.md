# 记忆模块（memory）接入方案

> 面向对象：`internal/memory/`（session.go + short.go，共 197 行）
> 现状：**整个包 0 引用**，数据库里 `Session`/`Message`/`Summary` 三张表也从未被 `AutoMigrate` 创建过。
> 本文分两部分：**怎么接进 agent 回答流程**，以及**接之前必须先修的东西**。

---

## 一、现状盘点

### 1.1 agent 侧：一条无状态的单轮链路

```
handler.Ask          c.GetString("username") → svc.Ask(req.Query, username, 10)
  └─ AgentService.Ask
       ├─ 每请求构造 ragRetriever + fileRetriever
       ├─ agent.NewAgent(...) 构造 agent
       ├─ messages = [SystemMessage(prompt), UserMessage(query)]    ← 只有这两条
       ├─ runner.Run(ctx, messages)
       └─ goroutine: 把事件流里的正文 chunk 转发给前端（SSE）
```

没有会话概念：请求里没有 `session_id`，`dto.AskReq` 只有 `Query`，模型每轮都从零开始。

### 1.2 memory 侧：写好了但接不上

| 文件 | 内容 | 能不能用 |
|---|---|---|
| `session.go` | 长期记忆（MySQL）：会话与消息的增删查、摘要存取 | ❌ 见 §5、§6 |
| `short.go` | 短期记忆（Redis）：20 条消息窗口 + 摘要 | ❌ 见 §5、§7 |

---

## 二、核心障碍：消息类型对不上

这是**第一个必须解决的问题**，其余都是次要的。

### 2.1 你的 agent 用的是 `*schema.Message`

`adk` 里的关键类型都是带默认类型参数的别名：

```go
type Runner               = TypedRunner[*schema.Message]
type ChatModelAgentConfig = TypedChatModelAgentConfig[*schema.Message]
type ResumableAgent       = TypedResumableAgent[*schema.Message]
```

所以 `runner.Run(ctx, messages)` 吃的、事件流里吐的，全是 `*schema.Message`。

### 2.2 memory 存的是 `*schema.AgenticMessage`

```go
// internal/memory/session.go:19,20
GetMessages(ctx, userID, sessionID string, limit int) ([]*schema.AgenticMessage, error)
AppendMessage(ctx, userID, sessionID string, message model.Message) error
```

这两个结构在 eino v0.9.19 里是**完全不同的两套表示**，而且**没有任何互转函数**（我翻过 `schema` 包，只有各自的构造器）：

| | `schema.Message`（agent 在用） | `schema.AgenticMessage`（memory 在存） |
|---|---|---|
| 角色 | `RoleType` | `AgenticRoleType` |
| 正文 | `Content string` | `ContentBlocks []*ContentBlock` |
| 工具调用 | `ToolCalls []ToolCall` + `ToolCallID`/`ToolName` | 混在 ContentBlocks 里（`function_tool_call` 等类型） |
| 推理内容 | `ReasoningContent string` | `ContentBlockTypeReasoning` 块 |
| 其他 | `MultiContent`、`Extra` | `ResponseMeta`、`Extra` |

### 2.3 三条路，选 A

| 方案 | 做法 | 代价 |
|---|---|---|
| **A. 改 memory 的存储类型**（推荐） | 把 `*schema.AgenticMessage` 全换成 `*schema.Message` | 只改 memory 包内部，零转换成本 |
| B. 手写双向转换 | 自己定义 `ContentBlocks ↔ Content`、`tool call block ↔ ToolCalls` 的映射 | 容易丢信息，eino 升级还得跟着改 |
| C. 整条链换成 Agentic 版 | `NewTypedChatModelAgent[*schema.AgenticMessage]` + `NewTypedRunner[*schema.AgenticMessage]` | prompt 构造、工具、流式事件处理全要跟着改；Agentic 是给多模态/多供应商表示准备的，存纯文本对话属于过度设计 |

**选 A**。你只需要存"用户问了什么、助手答了什么"，`*schema.Message` 完全够用，而且它就是事件流里现成拿到的类型。

---

## 三、接入的三个动作

位置都在 `internal/service/agent.go` 的 `Ask` 里。顺序：**读历史 → 拼消息 → 跑 → 落库**。

### 3.1 读

```go
history, err := svc.session.GetMessages(ctx, userID, sessionID, 20)
```

### 3.2 拼

```go
messages := make([]*schema.Message, 0, len(history)+2)
messages = append(messages, schema.SystemMessage(prompt))   // ① system 永远在最前
messages = append(messages, history...)                     // ② 历史在中间
messages = append(messages, schema.UserMessage(query))      // ③ 当前问题在最后
```

**顺序不能错**：历史放到 system 前面会顶掉指令的优先级，模型容易不遵守 prompt 里的规则。

### 3.3 写

写分两次：

```go
// 用户消息：在跑之前就写，这样即使后面失败/超时，用户的问题也不会丢
_ = svc.session.AppendMessage(ctx, userID, sessionID, userMsg)

result := runner.Run(ctx, messages)

go func() {
    defer close(output)
    var answer strings.Builder          // ← 边转发边累积（现在没有累积）
    defer func() {
        if answer.Len() > 0 {
            // 用 WithoutCancel：客户端可能已经断开，ctx 已被取消，
            // 但这条回复仍然要落库
            _ = svc.session.AppendMessage(context.WithoutCancel(ctx), userID, sessionID, assistantMsg)
        }
    }()
    for { /* 现有循环，转发的同时 answer.WriteString(msg.Content) */ }
}()
```

三个要点：

1. **助手回复是流式拼出来的**，现在的循环把 `msg.Content` 直接写进 channel 就丢了，必须同时累积。
2. **用 `defer` 落库**，保证流中途断掉也会写。
3. **`context.WithoutCancel`**（Go 1.21+）：客户端拿到最后一个 chunk 就断开是很常见的，此时 `ctx` 已取消，直接用它会写库失败。这个函数保留 ctx 里的值但去掉取消信号。

---

## 四、四个前置依赖

### 4.1 全链路没有 sessionID

| 位置 | 现状 |
|---|---|
| `dto.AskReq` | 只有 `Query`，没有 `SessionID` |
| `handler/agent.go:37` | `h.svc.Ask(req.Query, username, 10)` |
| `service/agent.go:35` | `Ask(query string, username string, TopK int)` |

要改：请求带 `session_id`（为空则新建会话并把新 ID 返回给前端）；`Ask` 的返回值里要能带上 sessionID。**这是接口契约变更，前端要配合。**

### 4.2 `Ask` 把 ctx 覆盖掉了

```go
// service/agent.go:36
ctx := context.Background()   // ← 客户端断开后不会取消下游调用
```

存历史要用 ctx 写 DB，改成从 handler 传 `c.Request.Context()` 进来。

### 4.3 `AgentService` 拿不到 db

它现在只有 `qdrant / embedder / chatmodel / silo`。`SessionManager` 需要 `*gorm.DB`，得从 `cmd/server/cmd.go` → `router` → `handler` → `service` 一路注入（和之前给 silo 做的一样）。

> 顺带：`AgentService.chatmodel` 字段现在存了但没人用，`agent.NewAgent` 内部又用 `os.Getenv` 重建了一个。这次一起理顺。

### 4.4 表还没建

```go
// internal/database/database.go:27
db.AutoMigrate(&model.User{})   // 只有 User
```

`Session` / `Message` / `Summary` 三张表不存在，任何 DB 操作都会报 "table doesn't exist"。接之前先加进去。

---

## 五、memory 包必须先修的东西

### 5.1 两个结构体都无法实例化（硬阻塞）

```go
type SessionManager struct {
	db    *gorm.DB   // 私有
	limit int        // 私有
}
type ShortMessageManager struct {
	redis  *redis.Client   // 私有
	Window int
}
```

**包内没有任何 `func New...` 构造函数**，外部包既不能直接构造（字段私有），也没有工厂函数。所以这个包现在**从外部根本创建不出来**，无论你的接入代码怎么写都编译不过。

要加：

```go
func NewSessionManager(db *gorm.DB, limit int) *SessionManager
func NewShortMessageManager(client *redis.Client, window int) *ShortMessageManager
```

### 5.2 `session.go` 的问题

| 行 | 问题 |
|---|---|
| 61 / 71 | `Table("session")` —— 表名 `session` |
| 71 | `DeleteSession` **没有 Where**，GORM 默认会直接报 `WHERE conditions required`，此方法永远失败；而且签名只有 `sessionID`，没有 `userID`，加上 Where 之前先想清楚越权问题（否则任何登录用户能删任意会话） |
| 80 | `Table("sesssion")` —— **表名拼错（三个 s）**，而且没有 Where，`Scan` 会随机取一行来判 `Status` |
| 87 | `Table("messages")` —— **5 处显式 `Table()` 用了 4 种表名**：`session`（61/71）、`sesssion`（80，拼错的）、`messages`（87）、`sessions`（99）；32/42 行走结构体推断，落到 `summaries`。同一份文件里五种表名混着用，其中 `session` 和 `sesssion` 这两种明显是错的 |
| 87 | `Scan(&messages)` 把数据库行扫进 `[]*schema.AgenticMessage` —— 字段根本对不上（见 §2） |
| 96 | `ListSessions` 收了 `limit` 参数但**没用**，返回全部会话 |
| 50 | `AppendMessage` 收了 `userID` 但在函数体里**没用到**（只 `db.Create(message)`），而 `GetMessages` 又按 `user_id` 过滤——但 `model.Message` 里根本没有 UserID 字段 |
| 60 | `CreateSession` 返回 `[]*schema.AgenticMessage` 但永远 `return nil, nil`，返回值没有意义 |
| 16 | 接口方法 `CreateSession(session model.Session)` **没有 ctx**，和同文件其它方法不一致 |
| 42 | `GetSummary` 注释说"选择最新的那一条"，但用的 `First()` 既没有 `ORDER BY`，`model.Summary` 也**没有主键**。GORM 的 `First()` 在无主键时会**回退到按第一个字段排序**（这里是 `user_id`），所以拿到的是"按 user_id 排第一的那条"，跟时间无关——同一个用户多条摘要时结果不可预期。想取最新得显式 `Order("created_at DESC")` |

**建议**：表名统一由结构体决定（给 `model.Session`/`Message`/`Summary` 加 `TableName()` 方法或 gorm tag），不要再手写 `Table("...")`。

### 5.3 `short.go` 的问题

| 行 | 问题 |
|---|---|
| 47 | `LRange(ctx, key, 1, 20)` —— **从下标 1 开始，第 1 条消息永远读不到**。应该用 `0, -1`（写入时已经 Trim 过了）或 `-20, -1` |
| 33 / 47 / 78 / 89 | 窗口大小 `20` **硬编码了四处**，而 `Window` 字段从来没人用 |
| 78 / 89 | `LTrim(key, 20, -1)` 注释写的是"删除队列里面的消息"，实际语义是**保留下标 20 之后的内容**。只是因为列表恰好 ≤ 20 条才等价于清空——一旦窗口大小改了就会留下残留。想清空应该用 `Del` |
| 31 | `jsonData, _ := json.Marshal(message)` —— 错误被丢弃 |
| 66 | `summary, _ = s.redis.Get(ctx, key).Result()` —— 注释说"如果错误的话直接返回空的就行"，但这个写法会把**连接失败**也吞掉，无法区分"没有摘要"和"Redis 挂了" |
| 72 / 83 | `UpdateSummary` 和 `CreateSummary` 的实现**几乎完全相同**（都是 Set + LTrim），可以合并 |
| 全部 | **摘要的触发时机没有任何地方实现**——`CreateSummary`/`UpdateSummary` 有实现但没有调用者，"什么时候该压缩历史"这个决策是空的 |
| 30 / 65 / 73 / 84 | key 是 `sessionID + "chat"` / `sessionID + "SUM"`，**没有命名空间前缀**，容易和别的 key 撞。建议 `rag:chat:{id}` / `rag:sum:{id}` |

---

## 六、数据模型要重做

```go
// internal/model/message.go
type Message struct {
	SessionID string
	Msg       *schema.AgenticMessage   // ①
}
```

三个问题：

1. **没有主键、没有时间戳、没有顺序字段** → 同一秒写入的多条消息，查询顺序不稳定。多轮对话对顺序极其敏感，必须有自增 ID。
2. **`Msg` 是 eino 类型** → GORM 不知道怎么落成列。就算换成 `*schema.Message`，里面的 `ToolCalls []ToolCall` 是切片、`Extra map[string]any` 是 map，都无法直接映射。
3. **没有 role 字段** → 想按角色过滤或前端渲染时没法用。

`model.Session` 也缺 `CreatedAt`/`UpdatedAt`——侧边栏要按"最后活跃时间"排序的话得有。

**建议的表结构：**

```
sessions
  id          VARCHAR(36)  PRIMARY KEY   -- 会话 ID（uuid）
  user_id     VARCHAR(64)  INDEX
  title       VARCHAR(128)               -- 前端侧边栏显示，可以取第一句话
  status      TINYINT(1)   DEFAULT 1     -- 1 正常 / 0 已删除
  created_at  DATETIME
  updated_at  DATETIME     INDEX         -- 侧边栏按它倒序

messages
  id          BIGINT AUTO_INCREMENT PRIMARY KEY   -- 顺序保证
  session_id  VARCHAR(36)  INDEX
  user_id     VARCHAR(64)  INDEX                  -- 冗余一份，按用户查时不用 join
  role        VARCHAR(16)                         -- user / assistant / system
  content     MEDIUMTEXT                          -- 正文，展示与直接查询用
  payload     JSON                                -- 完整消息（含 ToolCalls/Extra），回放用
  created_at  DATETIME     INDEX

summaries
  id          BIGINT AUTO_INCREMENT PRIMARY KEY
  user_id     VARCHAR(64)  INDEX
  session_id  VARCHAR(36)  INDEX
  summary     TEXT
  created_at  DATETIME
```

GORM 侧：`payload` 列用 `serializer:json` tag（或 `gorm.io/datatypes` 的 `JSON` 类型），这样 `*schema.Message` 能整条序列化进去，取出来也是完整的。

---

## 七、Redis 那层要不要保留？

我的建议：**第一版不做**。

理由：

- 需求只是"取最近 N 条喂给模型"，MySQL 一条 `ORDER BY id DESC LIMIT 20` 就够了
- Redis 的价值在**高频读**和**摘要缓存**，你这个场景大概率用不上
- §5.3 里那些问题（off-by-one、硬编码窗口、摘要没触发）说明这层从来没跑通过，接它等于同时接一堆未验证的逻辑
- 两套存储会带来一致性问题：MySQL 写成功了 Redis 写失败怎么办？反过来呢？

**做法**：把窗口大小当成 SQL 的 `LIMIT` 参数（配置项），历史和摘要都只走 MySQL。等真的遇到性能瓶颈再加 Redis——那时候你会更清楚该缓存什么。

如果坚持要 Redis 层，先把 §5.3 的问题修完，并且明确"摘要什么时候触发"这个策略。

---

## 八、存什么：工具调用轨迹要不要存？

`service/agent.go` 的循环里有一句：

```go
if mo.Role == schema.Tool {
	continue      // 工具消息被跳过了
}
```

说明事件流里混着工具调用和工具结果。存历史时两个选择：

| 方案 | 好处 | 代价 |
|---|---|---|
| **只存 user + assistant 的最终文本**（推荐第一版） | 历史干净、token 便宜 | 模型下一轮不记得自己调过什么工具 |
| 存完整轨迹（含 tool call / tool result） | 模型能"记得"检索过什么 | 每条历史很大，每轮都要重放，token 涨得快 |

先做第一个。等发现模型经常忘记上下文里的事实，再考虑第二个。

---

## 九、实施顺序

### 第 1 步：让 memory 能编译、能实例化
- 加 `NewSessionManager` / `NewShortMessageManager`
- `*schema.AgenticMessage` 全部换成 `*schema.Message`
- 修表名不一致（统一走 GORM 的 `TableName()`）

### 第 2 步：定表结构
- 重写 `model.Message`（自增 ID + role + content + payload JSON）
- `model.Session` 补 `CreatedAt`/`UpdatedAt`
- 把三张表加进 `AutoMigrate`

### 第 3 步：打通链路
- `AgentService` 注入 `*SessionManager`
- `Ask` 改成接收 `ctx` 和 `sessionID`，返回 sessionID
- `dto.AskReq` 加 `SessionID`；`handler.Ask` 透传 `c.Request.Context()`
- 实现"读历史 → 拼 messages → 跑 → 累积 → 落库"

### 第 4 步：前端配合
- 请求带 `session_id`
- 首次请求没带时，接收响应里返回的新 sessionID 并保存

### 第 5 步：验证

1. 发一个不带 `session_id` 的请求 → 响应里应该有个新 ID
2. 带上这个 ID 发第二个请求，问一个**只有上一轮才知道**的事（比如"我刚才说的那个数字是多少"）→ 模型应该答得出来
3. 查库：`SELECT id, role, LEFT(content, 40) FROM messages WHERE session_id = '...' ORDER BY id` → 应该看到成对的 user / assistant，且顺序正确
4. **重启服务**再问一次 → 历史仍然生效（证明是持久化，不是内存里蒙对的）
5. 边界：同一会话并发发两个请求 → 观察消息顺序是否错乱（这是多轮对话最容易踩的坑，必要时按 session 加锁或串行化）

### 第 6 步（可选，之后再说）
- 摘要压缩（先想清楚触发时机）
- Redis 短期记忆（先想清楚为什么需要）

---

## 十、一句话总结

**先把类型统一（`AgenticMessage` → `Message`）并补上构造函数，否则这个包连编译都过不了；然后按「读历史 → 拼消息 → 跑 → 落库」四步接进 `Ask`，只走 MySQL，别急着上 Redis。**
